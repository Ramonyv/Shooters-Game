# Bird Blast

Playable Scene 01 — Lakeside Village, built from the supplied Figma artwork.

```bash
npm install
npm run dev
```

Open the local URL printed by Vite. Mouse movement aims and click fires; on touch, drag to aim and release to fire. Press `Escape` to pause or resume. Add `?debug` to the URL during development to show hitboxes and live coordinates.
